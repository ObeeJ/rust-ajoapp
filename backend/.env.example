# Copy to backend/.env and fill in real values. Never commit the real .env file.

# Required — generate with: openssl rand -hex 32
JWT_SECRET=

# Required — from your Paystack dashboard
PAYSTACK_SECRET_KEY=

# Required — your deployed frontend URL (used in Paystack callback)
APP_URL=https://cowri.app
